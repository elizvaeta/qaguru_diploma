[pytest]
addopts =
    --alluredir=allure-results

markers =
    api_test: run only api tests
    ui_test: run only ui tests

log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_level=INFO
log_cli=true